[project]
name = "financial-planner"
version = "0.1.0"
description = "A new pywire application"
requires-python = ">=3.12"
dependencies = [
    "pywire",
    "stripe>=7.0.0",
    "sqlalchemy>=2.0.0",
]
